const ErrorMessage = ({ message, width, length, mertic }) => {
  
  return (
    
    
    <>
      
      <div className="alert alert-warning">
        <h1>{message}</h1>
      </div>
    </>
  )
}

export default ErrorMessage;